// tupla é uma especie de array que setamos a quantidade de elementos e os valores
var usuario = ['adson', 'luis', 18];
console.log(usuario);
usuario[0] = 'anne';
console.log(usuario);
// as tuplas podem mudar o valor contando que sigam as regras setadas
